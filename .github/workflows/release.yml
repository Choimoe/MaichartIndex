name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-tools:
    runs-on: windows-latest
    permissions:
      contents: write
      packages: write
      
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10' 
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install fastapi uvicorn pyinstaller
        
    - name: Build Tools
      run: |
        # Build Client (CLI)
        pyinstaller --onefile --name maichart-cli --add-data "static;static" main.py
        
        # Build Web (Server)
        pyinstaller --onefile --name maichart-webui --add-data "static;static" server.py
        
    - name: Organize Artifacts
      shell: pwsh
      run: |
        $TAG = "${{ github.ref_name }}"
        $ROOT = "MaichartIndex-Tools-$TAG"
        
        New-Item -ItemType Directory -Path "$ROOT" -Force
        
        Copy-Item "dist\maichart-cli.exe" -Destination "$ROOT\"
        Copy-Item "dist\maichart-webui.exe" -Destination "$ROOT\"
        
        Compress-Archive -Path "$ROOT" -DestinationPath "$ROOT.zip"
        
    - name: Create Release
      id: create_release
      uses: softprops/action-gh-release@v1
      with:
        files: MaichartIndex-Tools-${{ github.ref_name }}.zip
    
  build-db:
    runs-on: windows-latest
    permissions:
      contents: write
    
    env:
      PYTHONUTF8: 1
      
    steps:
    - uses: actions/checkout@v3
      with:
        submodules: recursive
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        
    - name: Build Database
      run: |
        # data folder should be present from checkout
        python main.py build
        
    - name: Compress Database
      shell: pwsh
      run: |
        $TAG = "${{ github.ref_name }}"
        $DB_ZIP = "MaichartIndex-Database-$TAG.zip"
        Compress-Archive -Path "maichart.db" -DestinationPath $DB_ZIP
        
    - name: Upload Database to Release
      uses: softprops/action-gh-release@v1
      with:
        files: MaichartIndex-Database-${{ github.ref_name }}.zip
